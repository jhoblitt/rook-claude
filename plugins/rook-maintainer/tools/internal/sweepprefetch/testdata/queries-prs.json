[
 "query { repository(owner: \"rook\", name: \"rook\") { pullRequests(states: OPEN, first: 25, orderBy: {field: CREATED_AT, direction: ASC}) { pageInfo { hasNextPage endCursor } nodes { \nnumber title state isDraft updatedAt createdAt\nauthor { login } authorAssociation baseRefName mergeable reviewDecision\nadditions deletions changedFiles\nlabels(first: 20) { nodes { name } }\nassignees(first: 10) { nodes { login } }\nfiles(first: 100) { pageInfo { hasNextPage endCursor } nodes { path } }\nlatestReviews(first: 20) { nodes { author { login } state } }\nreviewRequests(first: 20) { nodes { requestedReviewer {\n  ... on User { login } ... on Team { name } } } }\ncommits(last: 1) { nodes { commit { statusCheckRollup {\n  state contexts(first: 100) { pageInfo { hasNextPage endCursor } nodes { \n__typename\n... on CheckRun { name status conclusion\n  checkSuite { databaseId createdAt workflowRun { workflow { name } } } }\n... on StatusContext { context state }\n } } } } } }\n } } } }",
 "query { repository(owner: \"rook\", name: \"rook\") { pullRequests(states: OPEN, first: 25, after: \"PRCURSOR1\", orderBy: {field: CREATED_AT, direction: ASC}) { pageInfo { hasNextPage endCursor } nodes { \nnumber title state isDraft updatedAt createdAt\nauthor { login } authorAssociation baseRefName mergeable reviewDecision\nadditions deletions changedFiles\nlabels(first: 20) { nodes { name } }\nassignees(first: 10) { nodes { login } }\nfiles(first: 100) { pageInfo { hasNextPage endCursor } nodes { path } }\nlatestReviews(first: 20) { nodes { author { login } state } }\nreviewRequests(first: 20) { nodes { requestedReviewer {\n  ... on User { login } ... on Team { name } } } }\ncommits(last: 1) { nodes { commit { statusCheckRollup {\n  state contexts(first: 100) { pageInfo { hasNextPage endCursor } nodes { \n__typename\n... on CheckRun { name status conclusion\n  checkSuite { databaseId createdAt workflowRun { workflow { name } } } }\n... on StatusContext { context state }\n } } } } } }\n } } } }",
 "query { repository(owner: \"rook\", name: \"rook\") { pullRequest(number: 10) { commits(last: 1) { nodes { commit { statusCheckRollup { state contexts(first: 100) { pageInfo { hasNextPage endCursor } nodes { \n__typename\n... on CheckRun { name status conclusion\n  checkSuite { databaseId createdAt workflowRun { workflow { name } } } }\n... on StatusContext { context state }\n } } } } } } } } }",
 "query { repository(owner: \"rook\", name: \"rook\") { pullRequest(number: 10) { commits(last: 1) { nodes { commit { statusCheckRollup { state contexts(first: 100, after: \"CTXCURSOR1\") { pageInfo { hasNextPage endCursor } nodes { \n__typename\n... on CheckRun { name status conclusion\n  checkSuite { databaseId createdAt workflowRun { workflow { name } } } }\n... on StatusContext { context state }\n } } } } } } } } }",
 "query { repository(owner: \"rook\", name: \"rook\") { pullRequest(number: 10) { files(first: 100) { pageInfo { hasNextPage endCursor } nodes { path } } } } }",
 "query { repository(owner: \"rook\", name: \"rook\") { pullRequest(number: 10) { files(first: 100, after: \"FILECURSOR2\") { pageInfo { hasNextPage endCursor } nodes { path } } } } }"
]
