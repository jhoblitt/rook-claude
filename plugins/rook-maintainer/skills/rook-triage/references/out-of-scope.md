# Out-of-scope concepts — checked before any feature disposition

One entry per rejected CONCEPT (not per issue): what keeps being asked,
why rook declined, and the prior threads. Consult before labeling a
feature request or drafting a response — cite the entry instead of
re-litigating. Add an entry whenever a maintainer decision rejects a
concept likely to recur.

## Template

```text
## <concept>
- Ask: <what people request>
- Decision & why: <the standing answer, one or two lines>
- Prior: #N, #M
```

(No entries yet — populate as decisions land.)
